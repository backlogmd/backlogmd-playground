<!-- METADATA -->

```yaml
work: Foundations of the frontend React app
status: open
```

<!-- DESCRIPTION -->

Set up the React frontend for the Hamburger Rain dashboard: tooling, routing, API client, and base UI so we can build "My Rains" and "Trigger Rain" flows on top.

<!-- CONTEXT -->

Hamburger Rain API SaaS. Use Marmalade Design System where applicable. Frontend lives in repo (e.g. frontend/ or app/). API base URL and API key via env (VITE_*).
